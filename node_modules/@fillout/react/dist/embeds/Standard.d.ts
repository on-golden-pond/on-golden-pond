import React from "react";
import { FormParams } from "../embed.js";
type StandardProps = {
    filloutId: string;
    domain?: string;
    inheritParameters?: boolean;
    parameters?: FormParams;
    dynamicResize?: boolean;
};
export declare const Standard: ({ filloutId, domain, inheritParameters, parameters, dynamicResize, }: StandardProps) => React.JSX.Element;
export {};
