import React from "react";
import { FormParams } from "../embed.js";
import { SliderDirection } from "./Slider.js";
import { ButtonProps } from "../components/Button.js";
type SliderButtonProps = {
    filloutId: string;
    domain?: string;
    inheritParameters?: boolean;
    parameters?: FormParams;
    sliderDirection?: SliderDirection;
} & Omit<ButtonProps, "onClick">;
export declare const SliderButton: ({ filloutId, domain, inheritParameters, parameters, sliderDirection, text, color, size, float, }: SliderButtonProps) => React.JSX.Element;
export {};
