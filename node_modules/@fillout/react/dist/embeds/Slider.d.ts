import React from "react";
import { FormParams } from "../embed.js";
export type SliderDirection = "left" | "right";
type SliderProps = {
    filloutId: string;
    domain?: string;
    inheritParameters?: boolean;
    parameters?: FormParams;
    sliderDirection?: SliderDirection;
    onClose: () => void;
};
export declare const Slider: ({ filloutId, domain, inheritParameters, parameters, sliderDirection, onClose: _onClose, }: SliderProps) => React.ReactPortal;
export {};
