import React from "react";
import { FormParams } from "../embed.js";
type FullScreenProps = {
    filloutId: string;
    domain?: string;
    inheritParameters?: boolean;
    parameters?: FormParams;
};
export declare const FullScreen: ({ filloutId, domain, inheritParameters, parameters, }: FullScreenProps) => React.JSX.Element;
export {};
