import React from "react";
import { FormParams } from "../embed.js";
type PopupProps = {
    filloutId: string;
    domain?: string;
    inheritParameters?: boolean;
    parameters?: FormParams;
    onClose: () => void;
};
export declare const Popup: ({ filloutId, domain, inheritParameters, parameters, onClose: _onClose, }: PopupProps) => React.ReactPortal;
export {};
