import React from "react";
import { FormParams } from "../embed.js";
import { ButtonProps } from "../components/Button.js";
type PopupButtonProps = {
    filloutId: string;
    domain?: string;
    inheritParameters?: boolean;
    parameters?: FormParams;
} & Omit<ButtonProps, "onClick">;
export declare const PopupButton: ({ filloutId, domain, inheritParameters, parameters, text, color, size, float, }: PopupButtonProps) => React.JSX.Element;
export {};
