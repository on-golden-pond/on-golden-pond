import React from "react";
type ButtonSize = "small" | "medium" | "large";
type ButtonFloat = "bottomRight" | "bottomLeft";
export type ButtonProps = {
    onClick?: () => void;
    text?: string;
    color?: string;
    size?: ButtonSize;
    float?: ButtonFloat;
};
export declare const Button: ({ onClick, text, color, size, float, }: ButtonProps) => React.JSX.Element;
export {};
